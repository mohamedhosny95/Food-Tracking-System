name: Run Bot

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: bot-runner
  cancel-in-progress: true

jobs:
  bot:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Release runner for repair publish
        run: echo "Temporary no-op to cancel the previous long-running bot job."
